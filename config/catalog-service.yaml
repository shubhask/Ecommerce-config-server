server:
  port: 0

spring:
  datasource:
    url: jdbc:mysql://${MYSQL_SERVER:mysql-ecom}:3306/ecommerce_catalog
    username: root
    password: password
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
